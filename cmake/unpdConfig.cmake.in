@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

if(EXISTS "${CMAKE_CURRENT_LIST_DIR}/unpdTargets.cmake")
    include("${CMAKE_CURRENT_LIST_DIR}/unpdTargets.cmake")
endif()

check_required_components(unpd)
